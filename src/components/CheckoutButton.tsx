// Legacy component — replaced by CheckoutLink.tsx (LemonSqueezy overlay)
// Kept to avoid import errors in any pages still referencing it.
export default function CheckoutButton() {
  return null;
}
