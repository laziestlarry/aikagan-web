# Social publishing build fix

Restores the runtime components required by the application layout so the verified direct-social-publishing release can compile and deploy without dropping pageview, web-vitals, or support-entry behavior.
